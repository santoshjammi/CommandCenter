{
  "name": "devkeys",
  "version": "1.0.0",
  "private": true,
  "description": "DevKeys — developer cheat sheets",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build && cp -r .next/static .next/standalone/.next/static && cp -r public .next/standalone/public",
    "start": "cd frontend && node .next/standalone/server.js"
  }
}
