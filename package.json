{
  "name": "devkeys",
  "version": "1.0.0",
  "private": true,
  "description": "DevKeys — developer cheat sheets",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && rm -rf .next/cache && npm run build && cp -r .next/static .next/standalone/.next/static && cp -r public/. .next/standalone/public/",
    "start": "node server.js"
  }
}
